# JCC管理アプリ 仕様書（Claude Code投入用）

## 背景・目的
JCCメンバー間の連絡は現在グループLINEで行っているが、メッセージが流れてしまい「いつ何を依頼されたか」が分からなくなる問題がある。
LINE＝コミュニケーションツールはそのまま残し、**本アプリ＝仕事の見える化専用ツール**として新規に開発する。

## 対象ユーザー
- JCC（Habitat for Humanity Japan 学生団体）メンバー、約33人

## 技術構成
- フロントエンド／バックエンド：**Next.js**
- データベース：**Supabase**（未登録の場合はGitHubアカウントで新規登録可）
- ホスティング：**Vercel**（既存アカウントあり）
- リポジトリ：GitHub 新規リポジトリを作成

## ログイン方式
- パスワード認証ではなく「合言葉（共通パスフレーズ）」を入力して入室
- 初回入室時に名前などの個人情報を入力してもらい、以降はブラウザ側で保持して発信者情報として使う
- 個人ごとのアカウント登録・ログイン機能は作らない

## チーム一覧（送信先・カレンダー色分け共通で使用）
- 全体
- 総務
- 制作
- 司会
- コンテンツ1
- コンテンツ2
- コンテンツ3
- インターン
- ＋個人名を直接指名することも可能（複数選択可）

---

## 機能① 依頼掲示板（最優先で実装）

### 依頼の登録
- 入力項目：タイトル・内容・期限・優先度（「すぐやって！」／「通常」の2段階）
- 送信先：チーム（複数選択可）または個人名を選択。**選択必須**（宛先なしでは登録不可）
- 添付ファイル・画像機能：なし（対応しない）

### ステータス
- 「進行中」「完了」の2段階のみ
- 「進行中→完了」への切り替えは**誰でも可能**（相互協力式）

### やりとり
- 各依頼に対してコメントを書ける
- 絵文字リアクションをつけられる

### 一覧表示
- 進行中の依頼：**優先度が高い順→期限が近い順**の複合ソートで表示
- 完了した依頼：別タブ（履歴）に移動して表示
- 「自分宛だけ見る」フィルターは不要。全件表示のみでOK

---

## 機能② ミーティング管理

### 表示形式
- カレンダー形式（週表示／月表示の切り替え可能）
- 依頼掲示板とは別タブで表示
- 全チームの会議を同じカレンダーにまとめて表示し、**チームごとに色分け**

### 会議の登録
- 登録・編集権限：全員が自由に登録・編集可能
- 入力項目：タイトル・日時・チーム（複数選択＋個人名指定可）・会議URL・議事録リンク・その他リンク
- 定例会議（毎週同じ曜日など）を**繰り返し設定で一括登録**できる機能を実装

### SOS機能（追加機能）
- 対象者：そうたろう・すばる・りゅうせい・なな の4人（固定メンバー）
- 会議登録時に「SOS」フラグをON/OFFできる。ON時、必須者の事前指定はしない（4人のうち誰かが対応すればよい）
- 会議詳細画面に「参加する」ボタンを設置。4人のうち誰かがタップすると参加者リストに名前が追加され、誰が参加するか一目で分かる
- カレンダー表示：通常は詳細画面を開けば参加状況が分かる仕様だが、**まだ誰も参加表明していないSOS会議はカレンダー上でも目立つ表示（色・アイコン等）にして見逃しを防止する**

---

## 機能③ thanksページ

### コンセプト
- 少しでも頑張った人を褒める・感謝を伝えるページ

### 送信方法
- 誰でも自由に、いつでも送信可能（依頼完了時の自動付与などは不要）
- 宛先：個人 or チーム（両方から選択可能）
- 入力方法：自由記述のメッセージのみ（定型文ボタンは不要）

### 表示形式
- 新しい順のフィード表示（一覧を流れる形式）

---

## 対象外にした機能
- 多数決ボタン：**実装しない**
- プッシュ通知（LINE通知等）：**実装しない**。新しい依頼が入ったらLINEに手動でURLを共有する運用とする
- ファイル・画像アップロード：**実装しない**

---

## 開発の進め方（提案）
1. Next.js＋Supabaseのプロジェクトを新規作成
2. まず「依頼掲示板」から実装（ログイン／合言葉機能もここで一緒に作る）
3. 動作確認できたら「ミーティング管理（カレンダー）」を追加
4. 最後に「thanksページ」を追加
5. Vercelにデプロイし、合言葉付きURLをJCCメンバーに共有

## Claude Codeへの依頼文（コピペ用）
```
上記の仕様書の内容でJCC向け管理Webアプリを作ってください。
Next.js + Supabase + Vercelの構成で、
まず「依頼掲示板」機能から実装してください。
ログインは合言葉方式で、初回に名前を入力させて保持する形にしてください。
```
